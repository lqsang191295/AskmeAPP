/**
 * All states initial should be implemented here, such as
 *  user
 *  context
 *  ...
 */
// import User from '../dtos/User';
// import UserUtils from '../utils/User';

// export default () => {
//   const localUser = UserUtils.getUser() || {};
//   const user = new User({
//     _id: localUser.userId,
//     name: localUser.name,
//     photo: localUser.photo
//   }).getUser();
//
//   return { user };
// };

export default () => ({});
